<div class="entity_audit_events"></div>

{{#if properties}}
  <table class="table">
    {{#each properties}}
      {{> detail/properties/item }}
    {{/each}}

    <tr class="buttons-row requires-EDIT">
      <td colspan="3" class="buttons">
        <button class="add-new-properties btn btn-mini btn-default">Add Property</button>
      </td>
    </tr>
  </table>
{{else}}
  <div class="nav-header">Properties<span class="badge loading"/></div>
{{/if}}
